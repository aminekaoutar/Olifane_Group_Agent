{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "cd backend && pip install -r requirements.txt && python -m uvicorn olifan_backend_api:app --host 0.0.0.0 --port $PORT",
    "numReplicas": 1
  }
}