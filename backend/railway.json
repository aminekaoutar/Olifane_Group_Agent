{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm run build",
    "installCommand": "cd frontend && npm install",
    "startCommand": "cd backend && python -m uvicorn olifan_backend_api:app --host 0.0.0.0 --port $PORT"
  }
}